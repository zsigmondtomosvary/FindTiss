<%= form_with(model: favorite_thesis, local: true) do |form| %>
  <% if favorite_thesis.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(favorite_thesis.errors.count, "error") %> prohibited this favorite_thesis from being saved:</h2>

      <ul>
      <% favorite_thesis.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :short %>
    <%= form.text_field :short %>
  </div>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
